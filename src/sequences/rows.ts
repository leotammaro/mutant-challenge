/**
 * Given a matrix, returns the matrix itself as an array of arrays,
 * where each sub-array is a row of the matrix.
 *
 * This function is not required but it is useful to be more readable.
 *
 * @param {string[][]} matrix - The input matrix.
 * @returns {string[][]} - The same matrix as the input.
 */
export const getRows = (matrix: string[][]): string[][] => {
  return matrix;
};
